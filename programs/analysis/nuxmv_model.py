class NuXmvModel:

    def __init__(self, name, vars, define, init, invar, trans):
        self.name = name
        self.vars = vars
        self.define = define
        self.init = init
        self.invar = invar
        self.trans = trans
